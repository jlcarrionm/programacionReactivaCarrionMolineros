import { TamanoTitulosDirective } from './tamano-titulos.directive';

describe('TamanoTitulosDirective', () => {
  it('should create an instance', () => {
    const directive = new TamanoTitulosDirective();
    expect(directive).toBeTruthy();
  });
});
